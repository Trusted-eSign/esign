export let trusted: any = window.trusted;
export let fs: any = window.fs;
export let os: any = window.os;
export let path: any = window.path;
export let archiver: any = window.archiver;
export let async: any = window.async;
export let DEFAULT_PATH: string = window.DEFAULT_PATH;
export let DEFAULT_CERTSTORE_PATH: string = window.DEFAULT_CERTSTORE_PATH;
export let HOME_DIR: string = window.HOME_DIR;
